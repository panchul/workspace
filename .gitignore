
# All sorts of private data that should not be added to the repo.
# PCI stuff, encryption, etc.
private
!private/.git_keep

# The projects that use workspace ecosystem. Sandboxes, etc.
# They usually have their own repositories
projects
!projects/.git_keep

# The projects that are not under version control, and of a lesser
# value - experimenting, trying out, running quick demos, etc.
projects_sandbox
!projects_sandbox/.git_keep

# The directory where installation files are downloaded.
# It acts as a cache to not need to download the same files unnecessarily.
install
!install/.git_keep

# For multi-node setup or complex sets of config files, the configs are being
# generated by other scripts. This is where they are deployed and used,
# almost like the regular 'config' folder, but not under git because they are
# derivatives.
config_generated
!config_generated/.git_keep

# The general rule for 'tmp' directories - if you don't remember what that is,
# it is safe to delete it.
tmp


# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Fortran module files
*.mod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# Vagrant
.vagrant
*.retry
ubuntu-*-cloudimg-console.log

# Mac stuff
.DS_Store

# IntelliJ
.idea

